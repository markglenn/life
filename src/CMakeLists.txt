include(FindPkgConfig)
pkg_search_module(SDL2 REQUIRED sdl2)

add_executable(life main.cpp)

INCLUDE_DIRECTORIES(${SDL2_INCLUDE_DIRS} ${SDL2IMAGE_INCLUDE_DIRS} ${CMAKE_SOURCE_DIR}/lib/glm)
TARGET_LINK_LIBRARIES(life ${SDL2_LIBRARIES} ${SDL2IMAGE_LIBRARIES})
